export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <div className="text-center">
        <h1 className="text-2xl font-semibold">XOrithm — Server Status (dev)</h1>
        <p className="mt-3 text-gray-600">Welcome — the app is ready for step 2.</p>
      </div>
    </main>
  )
}